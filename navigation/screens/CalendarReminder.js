import { View, Text } from "react-native";
import { Calendar } from "react-native-calendars";
export default function CalendarReminder({ navigation }) {
  return (
    <View>
      <Calendar />
    </View>
  );
}
